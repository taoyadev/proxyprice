# Wrangler configuration for Cloudflare Pages deployment
# Copy this file to wrangler.toml and fill in your values
# NOTE: wrangler.toml is in .gitignore - never commit it!

# The account ID is now loaded from CLOUDFLARE_ACCOUNT_ID environment variable
# Get your account ID from: https://dash.cloudflare.com > Workers & Pages > Overview

name = "proxyprice"
compatibility_date = "2024-01-01"

# Build configuration
[build]
command = "npm run build"
cwd = "./"

# Directory to deploy (relative to cwd)
[build.upload]
format = "directory"
path = "dist"

# Environment variables (set in Cloudflare dashboard or via wrangler secret)
# Run: wrangler secret put PUBLIC_SITE_URL
# Run: wrangler secret put PUBLIC_FEEDBACK_URL
