---
import BaseLayout from "../layouts/BaseLayout.astro";
import { DATA_LAST_UPDATED_LONG, FEEDBACK_URL, SITE_URL, SITE_NAME } from "../lib/site";
import providersData from "../data/providers.json";
import pricingData from "../data/pricing.json";

// Calculate metrics
const totalProviders = providersData.total_count;
const totalPricingRecords = pricingData.pricing.length;
const comparableRecords = pricingData.pricing.filter(p => p.comparable).length;
const verificationRate = Math.round((comparableRecords / totalPricingRecords) * 100);
const proxyTypes = ['residential', 'datacenter', 'mobile', 'isp'];
---

<BaseLayout
  title="About"
  description="ProxyPrice is an independent proxy comparison platform. We normalize pricing from 40+ providers to a standard $/GB metric. No affiliate rankings, no paid placements. Just real data to help you find the best deal."
>
  <div class="container" style="padding: 3rem 0;">
    <header class="page-header">
      <h1>About ProxyPrice</h1>
      <p class="page-subtitle">
        We built ProxyPrice because comparing proxy providers was painful. Every company uses different pricing models, making fair comparison nearly impossible. We fix that by normalizing everything to one standard: cost per gigabyte.
      </p>
    </header>

    <section class="mission-section">
      <div class="mission-content">
        <h2>The Problem We Solve</h2>
        <p>
          The proxy industry thrives on confusion. Provider A charges per GB, Provider B charges per IP with bandwidth caps, Provider C bundles traffic into monthly plans with rollover rules. Comparing them requires a spreadsheet and an hour of your time. That is absurd.
        </p>
        <p>
          We built ProxyPrice to cut through this noise. We collect official pricing from {totalProviders}+ providers, normalize it to a standard <strong>$/GB</strong> metric where possible, and present it in clean comparison tables. You get actionable data in seconds, not hours.
        </p>
      </div>
    </section>

    <section class="why-section">
      <h2>Why ProxyPrice?</h2>
      <div class="why-grid">
        <div class="why-card">
          <div class="why-icon why-icon-unbiased">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a.75.75 0 000 1.5h.253a.25.25 0 01.244.304l-.459 2.066A1.75 1.75 0 0010.747 15H11a.75.75 0 000-1.5h-.253a.25.25 0 01-.244-.304l.459-2.066A1.75 1.75 0 009.253 9H9z" clip-rule="evenodd" />
            </svg>
          </div>
          <h3>No Hidden Agenda</h3>
          <p>Many "review" sites are secretly owned by proxy companies or rank providers by affiliate payout. We are independent. Our rankings reflect actual pricing data, nothing else.</p>
        </div>

        <div class="why-card">
          <div class="why-icon why-icon-normalized">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M4.5 2A1.5 1.5 0 003 3.5v13A1.5 1.5 0 004.5 18h11a1.5 1.5 0 001.5-1.5V7.621a1.5 1.5 0 00-.44-1.06l-4.12-4.122A1.5 1.5 0 0011.378 2H4.5zm2.25 8.5a.75.75 0 000 1.5h6.5a.75.75 0 000-1.5h-6.5zm0 3a.75.75 0 000 1.5h6.5a.75.75 0 000-1.5h-6.5z" clip-rule="evenodd" />
            </svg>
          </div>
          <h3>Apples-to-Apples Comparison</h3>
          <p>Per-GB, per-IP, per-port, bundled plans. We normalize them all to <strong>$/GB</strong> where mathematically possible. When conversion would be misleading, we show pricing as-is with clear labels.</p>
        </div>

        <div class="why-card">
          <div class="why-icon why-icon-independent">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M9.661 2.237a.531.531 0 01.678 0 11.906.11l-.22.22a.531.531 0 01-.678 0l-.22-.22a.531.531 0 010-.678zm1.06 1.06a.531.531 0 00-.678 0l-.22.22a.531.531 0 000 .678l.22.22a.531.531 0 00.678 0l.22-.22a.531.531 0 000-.678zM8.315 4.284a.75.75 0 011.06 0l.531.531.531-.531a.75.75 0 111.06 1.06l-.531.531.531.531a.75.75 0 01-1.06 1.06l-.531-.531-.531.531a.75.75 0 01-1.06-1.06l.531-.531-.531-.531a.75.75 0 010-1.06zm.75 4.75a.75.75 0 01.75.75v4.5a.75.75 0 01-1.5 0v-4.5a.75.75 0 01.75-.75zM10 13a.75.75 0 01.75.75v1.5a.75.75 0 01-1.5 0v-1.5A.75.75 0 0110 13zm0-6a.75.75 0 01.75.75v1.5a.75.75 0 01-1.5 0v-1.5A.75.75 0 0110 7z" clip-rule="evenodd" />
              <path d="M10 2a8 8 0 100 16 8 8 0 000-16zM1 10a9 9 0 1118 0 9 9 0 01-18 0z" />
            </svg>
          </div>
          <h3>Transparent Methodology</h3>
          <p>We document exactly how we collect, normalize, and present data. No black boxes, no secret sauce. If you spot an error, submit a correction and we will fix it.</p>
        </div>

        <div class="why-card">
          <div class="why-icon why-icon-updated">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd" />
            </svg>
          </div>
          <h3>Current Data</h3>
          <p>Proxy pricing changes constantly. We monitor providers and update our database regularly. You will never compare against stale data from six months ago.</p>
        </div>
      </div>
    </section>

    <section class="metrics-section">
      <h2>Trust Metrics</h2>
      <div class="metrics-grid">
        <div class="metric-card">
          <div class="metric-value">{totalProviders}+</div>
          <div class="metric-label">Providers Tracked</div>
        </div>
        <div class="metric-card">
          <div class="metric-value">{totalPricingRecords}</div>
          <div class="metric-label">Pricing Records</div>
        </div>
        <div class="metric-card">
          <div class="metric-value">{verificationRate}%</div>
          <div class="metric-label">Price Verified</div>
        </div>
        <div class="metric-card">
          <div class="metric-value">{proxyTypes.length}</div>
          <div class="metric-label">Proxy Types</div>
        </div>
      </div>
    </section>

    <section class="content">
      <h2>What We Track</h2>
      <ul>
        <li><strong>4 proxy types:</strong> Residential, datacenter, mobile, and ISP proxies from {totalProviders}+ providers worldwide.</li>
        <li><strong>{totalPricingRecords} pricing records:</strong> Individual tiers, plans, and bundles normalized for comparison.</li>
        <li><strong>$/GB conversion:</strong> Where mathematically possible, we convert all pricing to cost-per-gigabyte for fair sorting.</li>
        <li><strong>Price calculator:</strong> Estimate your monthly costs based on expected bandwidth usage.</li>
      </ul>

      <h2>Data Freshness</h2>
      <p>
        Our pricing database was last synchronized on <strong>{DATA_LAST_UPDATED_LONG}</strong>. We actively monitor major providers for pricing changes and update within 1-2 weeks of any significant adjustments.
      </p>

      <h2>Who Maintains This</h2>
      <p>
        ProxyPrice was built by developers who got tired of the proxy comparison grind. We needed to evaluate dozens of providers for web scraping projects and found no reliable, unbiased resource. So we built one.
      </p>
      <p>
        This is an independent project. We are not affiliated with any proxy provider. We do use affiliate links where available, but affiliate status never influences rankings or data presentation. Providers are ranked purely by their normalized pricing.
      </p>

      <h2>Accuracy Disclaimer</h2>
      <p>
        Proxy pricing changes frequently. While we strive for accuracy, always verify current pricing on the provider's official website before making a purchase decision. If you find incorrect data, please report it. We take accuracy seriously and will correct errors promptly.
      </p>
      {FEEDBACK_URL && (
        <p>
          <a href={FEEDBACK_URL} class="cta-link">Report a correction on GitHub →</a>
        </p>
      )}
    </section>

    <section class="cta-section">
      <h2>Stop Overpaying for Proxies</h2>
      <p>Compare real pricing from {totalProviders}+ providers. Find the best $/GB rate for your use case in minutes, not hours.</p>
      <div class="cta-buttons">
        <a href="/providers" class="btn btn-primary">Compare All Providers</a>
        <a href="/calculator" class="btn btn-secondary">Calculate Your Costs</a>
      </div>
    </section>
  </div>
</BaseLayout>

<style>
  .page-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .page-subtitle {
    font-size: 1.125rem;
    color: var(--color-text-light);
    max-width: 800px;
    margin: 1rem auto 0;
  }

  .mission-section {
    background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 100%);
    border-radius: 1rem;
    padding: 2.5rem;
    margin-bottom: 3rem;
  }

  .mission-content h2 {
    font-size: 1.75rem;
    margin-bottom: 1rem;
    color: #065f46;
  }

  .mission-content p {
    max-width: 800px;
    margin: 0 auto 1rem;
    line-height: 1.7;
  }

  @media (prefers-color-scheme: dark) {
    .mission-section {
      background: linear-gradient(135deg, #064e3b 0%, #065f46 100%);
    }

    .mission-content h2 {
      color: #d1fae5;
    }
  }

  .why-section {
    margin-bottom: 3rem;
  }

  .why-section h2 {
    text-align: center;
    margin-bottom: 2rem;
  }

  .why-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1.5rem;
  }

  .why-card {
    padding: 1.5rem;
    background: var(--color-bg-alt);
    border: 1px solid var(--color-border);
    border-radius: 0.75rem;
    text-align: center;
    transition: all 0.2s;
  }

  .why-card:hover {
    border-color: var(--color-primary);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
  }

  .why-icon {
    width: 3rem;
    height: 3rem;
    margin: 0 auto 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 0.75rem;
  }

  .why-icon svg {
    width: 1.5rem;
    height: 1.5rem;
    color: white;
  }

  .why-icon-unbiased {
    background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
  }

  .why-icon-normalized {
    background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);
  }

  .why-icon-independent {
    background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
  }

  .why-icon-updated {
    background: linear-gradient(135deg, #10b981 0%, #059669 100%);
  }

  .why-card h3 {
    margin-bottom: 0.75rem;
    font-size: 1.125rem;
  }

  .why-card p {
    color: var(--color-text-light);
    font-size: 0.9375rem;
    line-height: 1.6;
  }

  .metrics-section {
    margin-bottom: 3rem;
  }

  .metrics-section h2 {
    text-align: center;
    margin-bottom: 2rem;
  }

  .metrics-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
  }

  .metric-card {
    padding: 2rem 1.5rem;
    background: linear-gradient(135deg, #ecfdf5 0%, #d1fae5 100%);
    border: 1px solid #a7f3d0;
    border-radius: 0.75rem;
    text-align: center;
  }

  .metric-value {
    font-size: 2.5rem;
    font-weight: 700;
    color: #065f46;
    line-height: 1;
  }

  .metric-label {
    font-size: 0.875rem;
    color: #047857;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-top: 0.5rem;
  }

  @media (prefers-color-scheme: dark) {
    .metric-card {
      background: linear-gradient(135deg, #064e3b 0%, #065f46 100%);
      border-color: #047857;
    }

    .metric-value {
      color: #d1fae5;
    }

    .metric-label {
      color: #a7f3d0;
    }
  }

  .content {
    max-width: 860px;
    margin: 0 auto 3rem;
  }

  .content h2 {
    margin: 2rem 0 0.75rem;
  }

  .content p,
  .content li {
    color: var(--color-text);
  }

  .content ul {
    padding-left: 1.25rem;
  }

  .content li {
    margin-bottom: 0.5rem;
  }

  .cta-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--color-primary);
    font-weight: 600;
  }

  .cta-link:hover {
    text-decoration: none;
  }

  .cta-section {
    text-align: center;
    padding: 3rem 2rem;
    background: linear-gradient(135deg, var(--color-bg-alt) 0%, var(--color-bg) 100%);
    border-radius: 1rem;
    border: 1px solid var(--color-border);
  }

  .cta-section h2 {
    margin-bottom: 0.75rem;
  }

  .cta-section p {
    color: var(--color-text-light);
    margin-bottom: 1.5rem;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-block;
    padding: 0.875rem 2rem;
    border-radius: 0.5rem;
    font-weight: 600;
    transition: all 0.2s;
    text-decoration: none;
  }

  .btn-primary {
    background: var(--color-primary);
    color: white;
  }

  .btn-primary:hover {
    background: var(--color-primary-dark);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
  }

  .btn-secondary {
    background: transparent;
    color: var(--color-text);
    border: 2px solid var(--color-border);
  }

  .btn-secondary:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
  }

  @media (max-width: 768px) {
    .mission-section {
      padding: 1.5rem;
    }

    .metrics-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .why-grid {
      grid-template-columns: 1fr;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: stretch;
    }
  }
</style>
